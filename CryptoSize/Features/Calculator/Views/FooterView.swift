import SwiftUI

struct FooterView: View {
    var body: some View {
        Text("Be careful of leverage. It can go against you.")
            .font(.footnote)
            .foregroundColor(.gray)
            .padding(.bottom, 16)
    }
}
